// export const returnCheckItem = (existingLists) => {
//   const addToShoppingList = []

//   const whatever = [...existingLists].filter(
//     (entry) => 
//       [...entry[1].itemsList].filter(
//         (item) => item[1].addToShoppingList === true ? addToShoppingList.push([entry[0], item[0], item[1]]) : null
//       )
//   )

//   return addToShoppingList
// }